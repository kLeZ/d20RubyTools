<div class="table-responsive">
	<table id="members" class="table table-striped table-condensed table-hover">
	<% @room.members.each do |m| %>
		<tr>
			<td class="<%= 'success' if m.user.username == current_user.username %>">
			<% if m.is_online %>
				<%= image_tag('/assets/user-online.png', size: '16x16', alt: 'online') %>
			<% else %>
				<%= image_tag('/assets/user-offline.png', size: '16x16', alt: 'offline') %>
			<% end %>
				<span><%= m.user.username %></span>
			</td>
		</tr>
	<% end %>
	</table>
</div>
